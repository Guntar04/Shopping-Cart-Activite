#include <iostream>
#include <vector>
using namespace std;

int main()
{
    string usersPick;
    cout << "Enter the product name (or 'exit' to finish)";
	cin >> usersPick;

	switch (usersPick)
	{
	case 'exit':
	default:
		break;
	}
}